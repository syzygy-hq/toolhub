# Optional. Powers the shared server-side key for the YouTube Playlist Length
# Calculator (app/api/tools/youtube-playlist-length/route.ts). Get a free key
# from the Google Cloud Console with the "YouTube Data API v3" enabled.
# If left unset, visitors are prompted to paste their own free key instead —
# the site works either way.
YOUTUBE_API_KEY=
